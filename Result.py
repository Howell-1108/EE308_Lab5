def deResult( RealResult, UserResult ):
    #Check whether the real result which is calculated by computer is the same with that of users or not.
    if RealResult == UserResult:
        return 1
    else:
        return 0